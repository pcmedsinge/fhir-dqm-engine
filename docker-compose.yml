version: '3.9'

services:
  engine:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: development
      PORT: 3000
      LOG_LEVEL: debug
    # TODO Phase 2: add FHIR_BASE_URL and wire in hapi-fhir service
    # depends_on:
    #   - hapi-fhir

  # TODO Phase 2: uncomment and configure HAPI FHIR server
  # hapi-fhir:
  #   image: hapiproject/hapi:latest
  #   ports:
  #     - '8080:8080'
  #   environment:
  #     hapi.fhir.default_encoding: json
  #     hapi.fhir.fhir_version: R4
